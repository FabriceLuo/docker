ARG D_DEVELOP_NAME=develop
ARG D_DEVELOP_VERSION=1.0

FROM ${D_DEVELOP_NAME}:${D_DEVELOP_VERSION}

ARG BUILD_ROOT="/home/build"
ARG SRC_DIR=${BUILD_ROOT}/vim
ARG DEFAULT_SHELL="/bin/bash"

COPY --chown=root:root src ${SRC_DIR}

RUN make -C ${SRC_DIR} all

